default: &default
  adapter: postgresql
  encoding: unicode
  pool: <%= ENV.fetch("RAILS_MAX_THREADS") { 5 } %>

development:
  <<: *default
  database: npctown_development

test:
  <<: *default
  database: npctown_test

production:
  <<: *default
  url: <%= ENV["DATABASE_URL"] %>
