<!DOCTYPE html>
<html>
  <head>
    <title><%= content_for(:title) || "NPC Town" %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= vite_client_tag %>
    <%= vite_stylesheet_tag "application", media: "all" %>
    <%= vite_typescript_tag "inertia" %>
  </head>

  <body>
    <%= yield %>
  </body>
</html>
